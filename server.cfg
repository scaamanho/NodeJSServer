{
  "serverProtocol": "server protocol (http [Default] / https)",
  "serverProtocol": "https",
  
  "socketHost": "dns/ip donde corre el servidor",
  "socketHost": "localhost",

  "serverPort": "Puerto donde escucha el servidor",
  "serverPort": "8080"
}
